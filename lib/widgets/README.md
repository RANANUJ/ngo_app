# Widgets Structure

Group widgets by purpose/feature instead of keeping all widgets at one level.

Current groups:

- emergency: SOS and emergency-specific widgets

Add a new folder when a widget category grows, and keep shared widgets reusable.
