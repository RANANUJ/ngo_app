rules_version = '2';

// DEVELOPMENT RULES - Allow all reads and writes for testing
// WARNING: These rules are NOT secure for production!
// Replace with proper authentication-based rules before deploying to production.

service cloud.firestore {
  match /databases/{database}/documents {
    
    // NGO Registrations collection - allow all operations for development
    match /ngo_registrations/{registrationId} {
      allow read, write: if true;
    }
    
    // Fallback rule for any other collections during development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// ============================================================
// PRODUCTION RULES (use these when deploying to production):
// ============================================================
// 
// service cloud.firestore {
//   match /databases/{database}/documents {
//     
//     // NGO Registrations - anyone can submit, only admins can update
//     match /ngo_registrations/{registrationId} {
//       // Anyone can create a registration
//       allow create: if true;
//       
//       // Anyone can read their own registration (by email match or ID)
//       allow read: if true;
//       
//       // Only authenticated admins can update (approve/reject)
//       allow update: if request.auth != null && 
//                       request.auth.token.email == 'rana1452005@gmail.com';
//       
//       // No one can delete registrations
//       allow delete: if false;
//     }
//   }
// }
